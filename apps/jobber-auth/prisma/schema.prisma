generator client {
  provider = "prisma-client-js"
  output = "../../../generated/prisma-clients/jobber-auth"
}

datasource db {
  provider = "postgresql"
}

model User {
  id        Int     @default(autoincrement()) @id
  email     String  @unique
  password  String
}
